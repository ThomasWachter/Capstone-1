Este es la "base" de lo que podría ser nuestra app.

Tiene un archivo llamado apppy en donde cree la base de datos y dos paginas.
1: la principal
2: la que agrega clientes. Esto lo hace de manera manual, pero es solo para probar que este funcionando correctamente la base de datos que se creo y al parecer no tiene problema, ahi agregué a un cliente. este proceso es que habría que automaticar.

En segundo lugar. Los que trabajen en la parte de UX tienen los archivo en template que son los que determianan como se ven las paginas.
La principal ocupa el "first_page.html"
La segunda agregar_cliente.html

estas dos estan en abse con "base.html" que tiene un template inicial que tambien pueden modificar.

Insisto. esta es solo la base para que se pueda ir trabajando por encima.
